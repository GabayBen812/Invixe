import React from "react";
import Hammer from "./Hammer";

/** Hanging man: same shape as hammer, bearish (red). */
const HangingMan = (props: any) => <Hammer color="#E15637" {...props} />;

export default HangingMan;
