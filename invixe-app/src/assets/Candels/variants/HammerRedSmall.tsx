import React from "react";
import Hammer from "../Hammer";

const HammerRedSmall = (props: any) => (
  <Hammer color="#E15637" width={34} height={120} {...props} />
);

export default HammerRedSmall;


