import React from "react";
import Hammer from "../Hammer";

const HammerGreenSmall = (props: any) => (
  <Hammer color="#62D24C" width={34} height={120} {...props} />
);

export default HammerGreenSmall;


