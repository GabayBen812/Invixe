import React from "react";
import Hammer from "../Hammer";

const HammerRedTallNarrow = (props: any) => (
  <Hammer color="#E15637" width={34} height={140} {...props} />
);

export default HammerRedTallNarrow;


