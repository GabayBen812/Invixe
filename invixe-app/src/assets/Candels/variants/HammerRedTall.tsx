import React from "react";
import Hammer from "../Hammer";

const HammerRedTall = (props: any) => (
  <Hammer color="#E15637" width={44} height={140} {...props} />
);

export default HammerRedTall;


