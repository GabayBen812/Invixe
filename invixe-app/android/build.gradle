// Top-level build file where you can add configuration options common to all sub-projects/modules.

// Ensure SDK location exists for all subprojects (expo-modules-core, etc.)
def localPropsFile = file("local.properties")
if (!localPropsFile.exists()) {
  def sdkDir = System.getenv("ANDROID_HOME") ?: System.getenv("ANDROID_SDK_ROOT")
  if (sdkDir) {
    localPropsFile.text = "sdk.dir=" + sdkDir.replace("\\", "/") + "\n"
  } else {
    throw new GradleException("SDK not found. Create android/local.properties with: sdk.dir=C:/Users/YOUR_USER/AppData/Local/Android/Sdk\nOr set ANDROID_HOME to your Android SDK path.")
  }
}

buildscript {
  repositories {
    google()
    mavenCentral()
  }
  dependencies {
    classpath('com.android.tools.build:gradle')
    classpath('com.facebook.react:react-native-gradle-plugin')
    classpath('org.jetbrains.kotlin:kotlin-gradle-plugin')
  }
}

def reactNativeAndroidDir = new File(
  providers.exec {
    workingDir(rootDir)
    commandLine("node", "--print", "require.resolve('react-native/package.json')")
  }.standardOutput.asText.get().trim(),
  "../android"
)

allprojects {
  repositories {
    maven {
      // All of React Native (JS, Obj-C sources, Android binaries) is installed from npm
      url(reactNativeAndroidDir)
    }

    google()
    mavenCentral()
    maven { url 'https://www.jitpack.io' }
  }
}

apply plugin: "expo-root-project"
apply plugin: "com.facebook.react.rootproject"
